from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

# Create your views here.
def maintain (request):


        return render(request, template_name='maintain.html')